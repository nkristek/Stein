<Window x:Class="Stein.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Stein.ViewModels"
        xmlns:converters="clr-namespace:WpfBase.Converters;assembly=WpfBase"
        mc:Ignorable="d"
        Title="Stein - Silent Temp Installer" 
        Height="400" 
        MinHeight="300"
        Width="600"
        MinWidth="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Grid.IsSharedSizeScope="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" SharedSizeGroup="ButtonColumn"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto" SharedSizeGroup="ButtonColumn"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" 
               Content="Loading..."
               HorizontalAlignment="Center"
               VerticalAlignment="Center">
                <Label.Visibility>
                    <MultiBinding Converter="{x:Static converters:AnyBoolToVisibilityConverter.Instance}">
                        <MultiBinding.Bindings>
                            <Binding Path="RefreshApplicationsCommand.IsWorking"/>
                            <Binding Path="AddApplicationCommand.IsWorking"/>
                            <Binding Path="DeleteApplicationCommand.IsWorking"/>
                        </MultiBinding.Bindings>
                    </MultiBinding>
                </Label.Visibility>
            </Label>

            <Button Grid.Column="2"
                    Command="{Binding AddApplicationCommand}"
                    Content="Add Folder"
                    Padding="16,4"/>

            <Button Grid.Column="4"
                    Command="{Binding RefreshApplicationsCommand}"
                    Content="Refresh"
                    Padding="16,4"/>
        </Grid>

        <ScrollViewer Grid.Row="2" 
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl Grid.IsSharedSizeScope="True"
                          ItemsSource="{Binding Applications}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:ApplicationViewModel}">
                        <Border BorderThickness="1" 
                                CornerRadius="3" 
                                Padding="8" 
                                Margin="0,4" 
                                SnapsToDevicePixels="True"
                                BorderBrush="LightGray"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch">
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="ButtonColumn"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="SelectionColumn"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="ButtonColumn"/>
                                </Grid.ColumnDefinitions>

                                <!-- Label Column -->
                                <Grid Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" SharedSizeGroup="UpperDetailRow"/>
                                        <RowDefinition Height="8"/>
                                        <RowDefinition Height="Auto" SharedSizeGroup="LowerDetailRow"/>
                                    </Grid.RowDefinitions>

                                    <Label Grid.Row="0" 
                                           Content="{Binding Name}" />

                                    <Label Grid.Row="2"
                                           Content="{Binding Path}" />
                                </Grid>

                                <!-- Button Column -->
                                <Grid Grid.Column="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" SharedSizeGroup="UpperDetailRow"/>
                                        <RowDefinition Height="8"/>
                                        <RowDefinition Height="Auto" SharedSizeGroup="LowerDetailRow"/>
                                    </Grid.RowDefinitions>

                                    <Button Grid.Row="0"
                                            Content="Install"
                                            Padding="16,4"
                                            Command="{Binding InstallApplicationCommand}"/>

                                    <Button Grid.Row="2"
                                            Content="Uninstall"
                                            Padding="16,4"
                                            Command="{Binding UninstallApplicationCommand}"/>
                                </Grid>

                                <!-- Selection and SilentInstall Column -->
                                <Grid Grid.Column="4"
                                      IsEnabled="{Binding Parent.CurrentInstallation, Converter={x:Static converters:ValueNullToBoolConverter.Instance}}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" SharedSizeGroup="UpperDetailRow"/>
                                        <RowDefinition Height="8"/>
                                        <RowDefinition Height="Auto" SharedSizeGroup="LowerDetailRow"/>
                                    </Grid.RowDefinitions>

                                    <ComboBox Grid.Row="0"
                                              ItemsSource="{Binding InstallerBundles}"
                                              SelectedItem="{Binding SelectedInstallerBundle}"
                                              IsEnabled="{Binding InstallerBundles, Converter={x:Static converters:IEnumerableNotNullOrEmptyToBoolConverter.Instance}}"/>

                                    <CheckBox Grid.Row="2"
                                              VerticalAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              IsChecked="{Binding EnableSilentInstallation}">
                                        <Label Content="(un)install with defaults"/>
                                    </CheckBox>
                                </Grid>

                                <!-- Delete Button Column -->
                                <Grid Grid.Column="7">
                                    <Button Content="Delete"
                                            Padding="16,4"
                                            VerticalAlignment="Center"
                                            Command="{Binding Parent.DeleteApplicationCommand}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="4"
              Visibility="{Binding CurrentInstallation, Converter={x:Static converters:ValueNotNullToVisibilityConverter.Instance}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" 
                   Content="Installing..."
                   Visibility="{Binding CurrentInstallation.Type, Converter={x:Static converters:ObjectToStringEqualsParameterToVisibilityConverter.Instance}, ConverterParameter=Install}"/>

            <Label Grid.Column="0" 
                   Content="Uninstalling..."
                   Visibility="{Binding CurrentInstallation.Type, Converter={x:Static converters:ObjectToStringEqualsParameterToVisibilityConverter.Instance}, ConverterParameter=Uninstall}"/>

            <Label Grid.Column="2"
                   Content="{Binding CurrentInstallation.ProgressString}"/>

            <Label Grid.Column="4"
                   Content="{Binding CurrentInstallation.Name}"/>
        </Grid>
    </Grid>
</Window>
