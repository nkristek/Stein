<UserControl x:Class="Stein.Views.InstallerBundleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stein.Views"
             xmlns:viewModels="clr-namespace:Stein.ViewModels;assembly=Stein.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:InstallerBundleViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <TextBlock TextTrimming="CharacterEllipsis"
                   Text="{Binding Name}"/>
    </Grid>
</UserControl>
